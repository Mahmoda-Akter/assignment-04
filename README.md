//1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?

to caught unique id we use getElementById
to caught targeted class by the getElementsByClassName
to caught first html element we use querySelector
to caught all html elements we use querySelectorAll

//How do you create and insert a new element into the DOM?

first of all i need to create a element using createElement() methode and to insert we need to use appen/appendChile(),prepend

//3. What is Event Bubbling? And how does it work?

event bubbling is a event propagration 
when we click a targeted element it can reach it's parentNode like a tree root

/4. What is Event Delegation in JavaScript? Why is it useful?

event delegation is a event handling tachnicqe 
it's usefull because we do not need to add  addEventListner() again and aganin thai is whay  code clear and reduce redundenty in our program

//5. What is the difference between preventDefiault() and stopPropagation() methods?

preventDefault() : to stop browser default vehavior .
stopPropagation() : to stop a targeted element to going  his parentNode.

